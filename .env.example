# ==========================================
# AWID v3.0 - Variables d'environnement
# ==========================================

# Base de données PostgreSQL
POSTGRES_USER=awid_admin
POSTGRES_PASSWORD=change_me_super_strong_password
POSTGRES_DB=awid_v3

# PGAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=change_me_pgadmin_password

# JWT Secrets (générer des clés fortes : openssl rand -base64 64)
JWT_SECRET=your_super_long_jwt_secret_min_64_chars
JWT_REFRESH_SECRET=your_different_refresh_secret_min_64_chars

# MinIO (S3)
MINIO_ROOT_USER=awidminio
MINIO_ROOT_PASSWORD=change_me_minio_super_strong_password
MINIO_BUCKET=awid-uploads
